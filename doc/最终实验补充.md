两个实验文件均运行通过。整理一下完成的内容：

  ---
  创建的文件

  exp2_critic_sharing_analysis.py — 邻域共享效果测试

  分为两部分：

  Part 1 — 维度分析 (Table 4.2)
  - 固定 M=5，N ∈ {2,4,8,16,32,64,128}
  - 解析计算三种 critic 的输入维度（无需训练或创建环境）
  - 输出 results/exp2_critic_dim.csv + results/exp2_critic_dim.png（线性+对数双图）

  Part 2 — 推理延迟 (Table 4.3)
  - N ∈ {2,4,8,16,32,64}，每个 (N, algo) 组合测试 1000 次决策
  - 用合成 obs 直接构建模型，避免为大 N 启动重量级环境
  - 输出 results/exp2_inference_latency.csv + results/exp2_inference_latency.png

  python exp2_critic_sharing_analysis.py           # 完整运行
  python exp2_critic_sharing_analysis.py --dim-only   # 仅维度分析（秒级）
  python exp2_critic_sharing_analysis.py --lat-only --trials 200

  ---
  exp3_convergence_speed.py — 收敛速度对比实验

  对应论文 Fig 4.1：HierMARL vs CommonActor，N=4, M=5, 5 个随机种子，绘制均值 ±1 std 曲线。

  两种模式：
  - 默认：从头训练（5 seeds × 500 episodes × 2 算法）
  - --from-logs：读取已有的 reward.txt 或控制台日志文件（支持逗号分隔多路径）

  python exp3_convergence_speed.py                  # 从头训练 5 seeds
  python exp3_convergence_speed.py --episodes 300 --seeds 3
  # 使用已有日志（支持多 seed 路径）：
  python exp3_convergence_speed.py --from-logs \
      --hier-logs run1/reward.txt,run2/reward.txt \
      --ca-logs   run3/reward.txt,run4/reward.txt

  输出 results/exp3_convergence_speed.csv + results/exp3_convergence_speed.png