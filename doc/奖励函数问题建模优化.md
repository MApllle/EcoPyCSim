【此次修改被撤回】
  
  ---
  实现总结

  A1 — 拉格朗日约束 MADDPG

  新文件 schedulers/marl/maddpg/LagrangianMADDPG.py：
  - LagrangianBuffer：在 Buffer 基础上增加每步 constraint cost 存储
  - LagrangianMADDPG：覆写 add() / sample() / learn()；每次 learn() 后对共享的 λ 做对偶梯度上升 λ ← max(0, λ + η_λ ·
  (avg_cost − budget))

  环境改动（env/cloud_scheduling.py）：
  - info() 新增 energy_reward、constraint_cost、is_scheduling_step 三个字段供 Lagrangian MADDPG 读取

  ---
  A2 — 势能奖励塑形

  - 新构造参数 use_potential_shaping / shaping_gamma
  - _compute_potential()：对活跃（有负载）服务器计算 Jain 公平指数
  - step() 中在调度步骤注入塑形项 γΦ(s') − Φ(s)，保证策略不变性

  ---
  A3 — 异构感知注意力 Critic

  - HeterogeneousCritic（Agent.py）：从全局观测中分离出任务特征和每台服务器的 [cpu_util,
  efficiency_tier]，用交叉注意力让任务查询资源，再接 MLP 输出 Q 值
  - MADDPG._obs_offsets() 自动从 dim_info 推断特征索引，无需手动硬编码

  ---
  训练脚本开关（run_env_train_maddpg.py）

  USE_LAGRANGIAN=1 USE_SHAPING=1 USE_ATTENTION=1 uv run python run_env_train_maddpg.py
  三个开关可任意组合，结果目录名自动包含变体标签（如 maddpg_lagrangian_shaping_attention_...）。

✻ Worked for 12m 41s

※ recap: Three reward/modeling optimizations (Lagrangian constraint, potential shaping, attention critic) were
  implemented and tested. All pass smoke tests and can be toggled via environment variables when running the training
  script. (disable recaps in /config)